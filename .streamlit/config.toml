[theme]
# Default to dark mode, but user can switch via Settings menu
base = "dark"

# Custom colors (optional - these override the base theme)
primaryColor = "#f63366"
backgroundColor = "#0e1117"
secondaryBackgroundColor = "#262730"
textColor = "#fafafa"
